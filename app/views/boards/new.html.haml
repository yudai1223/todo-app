.container
  = form_with(model:@board , url: boards_path, method: 'post' ,local:true) do |f|
    %h2 New Board
    %ul
    %div
      = f.label :title, 'Name'
    %div
      = f.text_field :title, class: 'text'
    %div
      = f.label :content, 'Description'
    %div
      = f.text_area :content
    = f.submit 'submit', class: 'submit-color submit '